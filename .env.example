# Required for POST /webhook and POST /test-order.
WEBHOOK_SECRET=replace-with-a-long-random-secret

# Optional settings.
DB_PATH=database/trades.db
MT5_COMMENT=TV_EXEC_BRIDGE
MIN_RR=1.8
DEFAULT_VOLUME=0.01
